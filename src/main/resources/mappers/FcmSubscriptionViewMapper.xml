<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.goraebap.blog.infra.mapper.FcmSubscriptionViewMapper">

    <select id="findAll" resultType="FcmSubscriptionViewModel">
        select id
             , token
             , ip_address
             , browser
             , os
             , device_type
             , created_at
        from fcm_subscriptions
        where 1=1
        <if test="search != null and search != ''">
            and (
                ip_address ilike '%' || #{search} || '%'
                or browser ilike '%' || #{search} || '%'
                or os ilike '%' || #{search} || '%'
            )
        </if>
        order by created_at desc
        limit #{limit} offset #{offset}
    </select>

    <select id="countAll" resultType="int">
        select count(*)
        from fcm_subscriptions
        where 1=1
        <if test="search != null and search != ''">
            and (
                ip_address ilike '%' || #{search} || '%'
                or browser ilike '%' || #{search} || '%'
                or os ilike '%' || #{search} || '%'
            )
        </if>
    </select>

    <select id="countByBrowser" resultType="map">
        select coalesce(browser, '알 수 없음') as name
             , count(*) as count
        from fcm_subscriptions
        group by browser
        order by count desc
    </select>

    <select id="countByOs" resultType="map">
        select coalesce(os, '알 수 없음') as name
             , count(*) as count
        from fcm_subscriptions
        group by os
        order by count desc
    </select>

    <select id="countByDeviceType" resultType="map">
        select coalesce(device_type, '알 수 없음') as name
             , count(*) as count
        from fcm_subscriptions
        group by device_type
        order by count desc
    </select>

</mapper>
