<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.goraebap.blog.infra.mapper.BlockedIpViewMapper">

    <select id="findAll" resultType="BlockedIpViewModel">
        select id
             , ip_address
             , reason
             , blocked_by
             , expires_at::timestamp as expires_at
             , is_active_yn
             , created_at
             , updated_at
        from blocked_ips
        where 1=1
        <if test="search != null and search != ''">
            and (
                ip_address ilike '%' || #{search} || '%'
                or reason ilike '%' || #{search} || '%'
            )
        </if>
        order by created_at desc
        limit #{limit} offset #{offset}
    </select>

    <select id="countAll" resultType="int">
        select count(*)
        from blocked_ips
        where 1=1
        <if test="search != null and search != ''">
            and (
                ip_address ilike '%' || #{search} || '%'
                or reason ilike '%' || #{search} || '%'
            )
        </if>
    </select>

    <select id="findById" resultType="BlockedIpViewModel">
        select id
             , ip_address
             , reason
             , blocked_by
             , expires_at::timestamp as expires_at
             , is_active_yn
             , created_at
             , updated_at
        from blocked_ips
        where id = #{id}
    </select>

</mapper>
